# Optional extras (NOT required for the Tally plugin)

## Native plugin (use this)

Load: `tdl\BarcodeAddon.txt`  
Install font: see `fonts\README.md`

## Node.js API (ignore unless you need HTTP/PDF)

Still present at repo root as `services\barcode-api` (folder may be locked if the old API was running).  
It is **not** part of the native workflow.

If you want PDF generation later, see that folder’s README / `docs\api-reference.md`.
